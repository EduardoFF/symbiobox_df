How to perform a fresh install on an Odroid C1+

1) Install docker:

sudo apt-get install lxc aufs-tools cgroup-lite apparmor docker.io

2) Add odroid user to docker group

sudo groupadd docker
sudo gpasswd -a ${USER} docker
sudo service docker restart

3) install lcm - branch 1.3.1

4) install click

5) clone nccr_rgc_manet
   setup/make install_manet

6) install dnsmasq